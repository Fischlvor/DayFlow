# DayFlow 项目结构文档

## 概述

DayFlow 是一款基于 Android 原生开发的日历应用，采用 **MVVM + Clean Architecture** 架构，使用 **按功能分包（Feature-based）** 的目录结构组织代码。

## 技术栈

| 类别 | 技术选型 |
|------|---------|
| 语言 | Kotlin |
| UI 框架 | Jetpack Compose |
| 架构模式 | MVVM + Clean Architecture |
| 日历组件 | kizitonwose/Calendar |
| 数据库 | Room |
| 依赖注入 | Hilt |
| 异步处理 | Kotlin Coroutines + Flow |
| iCalendar | ical4j (RFC 5545) |

## 目录结构

```
app/src/main/java/com/hsk/dayflow/
│
├── app/                                # 🚀 应用入口模块
│   ├── DayFlowApplication.kt           # Application 类
│   ├── MainActivity.kt                 # 主 Activity
│   └── navigation/
│       └── NavGraph.kt                 # 导航图定义
│
├── core/                               # 🔧 核心公共模块
│   │
│   ├── common/                         # 公共工具和常量
│   │   ├── Constants.kt                # 全局常量
│   │   ├── DateUtils.kt                # 日期工具类
│   │   └── Extensions.kt               # Kotlin 扩展函数
│   │
│   ├── database/                       # Room 数据库
│   │   ├── DayFlowDatabase.kt          # 数据库定义
│   │   ├── Converters.kt               # 类型转换器
│   │   ├── dao/
│   │   │   ├── EventDao.kt             # 日程 DAO
│   │   │   └── CalendarDao.kt          # 日历 DAO
│   │   └── entity/
│   │       ├── EventEntity.kt          # 日程实体
│   │       └── CalendarEntity.kt       # 日历实体
│   │
│   ├── model/                          # 公共数据模型
│   │   ├── CalendarEvent.kt            # 日程事件模型
│   │   ├── CalendarDay.kt              # 日历日期模型
│   │   ├── ReminderType.kt             # 提醒类型枚举
│   │   └── ViewType.kt                 # 视图类型枚举
│   │
│   ├── ui/                             # 公共 UI 组件
│   │   ├── theme/
│   │   │   ├── Color.kt                # 颜色定义
│   │   │   ├── Theme.kt                # 主题定义
│   │   │   └── Type.kt                 # 字体定义
│   │   └── components/
│   │       ├── LoadingIndicator.kt     # 加载指示器
│   │       ├── ErrorView.kt            # 错误视图
│   │       └── EmptyView.kt            # 空状态视图
│   │
│   └── di/                             # 依赖注入
│       ├── AppModule.kt                # 应用级模块
│       ├── DatabaseModule.kt           # 数据库模块
│       └── RepositoryModule.kt         # 仓库模块
│
├── feature/                            # 📦 功能模块
│   │
│   ├── calendar/                       # 📅 日历视图功能
│   │   ├── CalendarScreen.kt           # 日历主界面
│   │   ├── CalendarViewModel.kt        # 日历 ViewModel
│   │   ├── CalendarUiState.kt          # UI 状态
│   │   ├── CalendarRepository.kt       # 日历仓库
│   │   └── components/
│   │       ├── MonthView.kt            # 月视图组件
│   │       ├── WeekView.kt             # 周视图组件
│   │       ├── DayView.kt              # 日视图组件
│   │       ├── CalendarHeader.kt       # 日历头部
│   │       ├── DayCell.kt              # 日期单元格
│   │       └── ViewSwitcher.kt         # 视图切换器
│   │
│   ├── event/                          # 📝 日程管理功能
│   │   ├── list/
│   │   │   ├── EventListScreen.kt      # 日程列表界面
│   │   │   └── EventListViewModel.kt   # 列表 ViewModel
│   │   ├── detail/
│   │   │   ├── EventDetailScreen.kt    # 日程详情界面
│   │   │   └── EventDetailViewModel.kt # 详情 ViewModel
│   │   ├── edit/
│   │   │   ├── EventEditScreen.kt      # 日程编辑界面
│   │   │   └── EventEditViewModel.kt   # 编辑 ViewModel
│   │   ├── EventRepository.kt          # 日程仓库
│   │   ├── components/
│   │   │   ├── EventItem.kt            # 日程列表项
│   │   │   ├── EventForm.kt            # 日程表单
│   │   │   ├── DateTimePicker.kt       # 日期时间选择器
│   │   │   ├── ColorPicker.kt          # 颜色选择器
│   │   │   └── ReminderPicker.kt       # 提醒选择器
│   │   └── usecase/
│   │       ├── GetEventsUseCase.kt     # 获取日程用例
│   │       ├── GetEventByIdUseCase.kt  # 获取单个日程
│   │       ├── AddEventUseCase.kt      # 添加日程用例
│   │       ├── UpdateEventUseCase.kt   # 更新日程用例
│   │       └── DeleteEventUseCase.kt   # 删除日程用例
│   │
│   ├── reminder/                       # ⏰ 提醒功能
│   │   ├── ReminderScheduler.kt        # 提醒调度器
│   │   ├── ReminderService.kt          # 提醒前台服务
│   │   ├── ReminderReceiver.kt         # 提醒广播接收器
│   │   ├── ReminderWorker.kt           # WorkManager Worker
│   │   └── NotificationHelper.kt       # 通知帮助类
│   │
│   └── settings/                       # ⚙️ 设置功能
│       ├── SettingsScreen.kt           # 设置界面
│       ├── SettingsViewModel.kt        # 设置 ViewModel
│       └── SettingsDataStore.kt        # 设置数据存储
│
└── extension/                          # 🔌 扩展功能模块
    │
    ├── icalendar/                      # 📤 导入导出功能
    │   ├── ICalendarParser.kt          # iCalendar 解析器
    │   ├── ICalendarGenerator.kt       # iCalendar 生成器
    │   ├── ImportScreen.kt             # 导入界面
    │   ├── ExportScreen.kt             # 导出界面
    │   ├── ICalendarViewModel.kt       # ViewModel
    │   └── usecase/
    │       ├── ImportEventsUseCase.kt  # 导入用例
    │       ├── ExportEventsUseCase.kt  # 导出用例
    │       └── ParseIcsFileUseCase.kt  # 解析 ICS 文件
    │
    ├── subscription/                   # 🌐 网络订阅功能
    │   ├── SubscriptionScreen.kt       # 订阅管理界面
    │   ├── SubscriptionViewModel.kt    # ViewModel
    │   ├── SubscriptionRepository.kt   # 订阅仓库
    │   ├── api/
    │   │   └── CalendarApi.kt          # 日历 API 接口
    │   └── usecase/
    │       ├── AddSubscriptionUseCase.kt
    │       ├── SyncSubscriptionUseCase.kt
    │       └── RemoveSubscriptionUseCase.kt
    │
    └── lunar/                          # 🌙 农历功能
        ├── LunarCalendar.kt            # 农历计算核心
        ├── LunarDate.kt                # 农历日期模型
        ├── LunarConverter.kt           # 公历农历转换
        ├── LunarFestival.kt            # 农历节日
        └── SolarTerm.kt                # 二十四节气
```

## 资源文件结构

```
app/src/main/res/
├── drawable/                           # 图片资源
│   ├── ic_launcher_*.xml               # 应用图标
│   ├── ic_calendar.xml                 # 日历图标
│   ├── ic_event.xml                    # 日程图标
│   ├── ic_reminder.xml                 # 提醒图标
│   └── ic_settings.xml                 # 设置图标
│
├── values/
│   ├── colors.xml                      # 颜色资源
│   ├── strings.xml                     # 字符串资源
│   ├── themes.xml                      # 主题资源
│   └── dimens.xml                      # 尺寸资源
│
├── values-night/
│   └── themes.xml                      # 深色主题
│
├── values-zh/
│   └── strings.xml                     # 中文字符串
│
└── xml/
    ├── backup_rules.xml                # 备份规则
    ├── data_extraction_rules.xml       # 数据提取规则
    └── file_paths.xml                  # 文件路径（分享用）
```

## 模块依赖关系

```
┌─────────────────────────────────────────────────────────────┐
│                          app/                                │
│                    (Application Entry)                       │
└─────────────────────────┬───────────────────────────────────┘
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│  feature/   │   │  feature/   │   │  feature/   │
│  calendar   │   │   event     │   │  reminder   │
└──────┬──────┘   └──────┬──────┘   └──────┬──────┘
       │                 │                 │
       └────────────────┼─────────────────┘
                        ▼
              ┌─────────────────┐
              │     core/       │
              │  (Shared Code)  │
              └─────────────────┘
                        ▲
                        │
       ┌────────────────┼────────────────┐
       │                │                │
┌──────┴──────┐  ┌──────┴──────┐  ┌──────┴──────┐
│ extension/  │  │ extension/  │  │ extension/  │
│  icalendar  │  │subscription │  │   lunar     │
└─────────────┘  └─────────────┘  └─────────────┘
```

## 数据流

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│     UI       │────▶│  ViewModel   │────▶│   UseCase    │
│   (Screen)   │◀────│              │◀────│              │
└──────────────┘     └──────────────┘     └──────────────┘
                                                 │
                                                 ▼
                                          ┌──────────────┐
                                          │  Repository  │
                                          └──────────────┘
                                                 │
                            ┌────────────────────┼────────────────────┐
                            ▼                    ▼                    ▼
                     ┌──────────────┐     ┌──────────────┐     ┌──────────────┐
                     │   Room DB    │     │  DataStore   │     │  Remote API  │
                     └──────────────┘     └──────────────┘     └──────────────┘
```

## 开发阶段

### Phase 1: 基础架构 (预计 2-3 天)
- [ ] 创建目录结构
- [ ] 配置 Gradle 依赖
- [ ] 实现 core/database
- [ ] 实现 core/model
- [ ] 配置 Hilt 依赖注入

### Phase 2: 日历视图 (预计 3-4 天)
- [ ] 实现 MonthView
- [ ] 实现 WeekView
- [ ] 实现 DayView
- [ ] 实现视图切换
- [ ] 日期选择交互

### Phase 3: 日程管理 (预计 2-3 天)
- [ ] 实现 EventRepository
- [ ] 实现 CRUD UseCase
- [ ] 实现日程列表界面
- [ ] 实现日程编辑界面
- [ ] 实现日程详情界面

### Phase 4: 提醒功能 (预计 2 天)
- [ ] 实现 ReminderScheduler
- [ ] 实现 AlarmManager 提醒
- [ ] 实现通知显示
- [ ] 处理设备重启

### Phase 5: 导入导出 (预计 2 天) [扩展]
- [ ] 实现 iCalendar 解析
- [ ] 实现 iCalendar 生成
- [ ] 实现文件选择/保存

### Phase 6: 农历支持 (预计 1-2 天) [扩展]
- [ ] 实现公历农历转换
- [ ] 实现农历节日
- [ ] 实现二十四节气

### Phase 7: 网络订阅 (预计 2-3 天) [扩展]
- [ ] 实现订阅 URL 解析
- [ ] 实现定时同步
- [ ] 实现订阅管理界面

## 命名规范

| 类型 | 命名规则 | 示例 |
|------|---------|------|
| Screen | `XxxScreen.kt` | `CalendarScreen.kt` |
| ViewModel | `XxxViewModel.kt` | `CalendarViewModel.kt` |
| Repository | `XxxRepository.kt` | `EventRepository.kt` |
| UseCase | `XxxUseCase.kt` | `AddEventUseCase.kt` |
| Entity | `XxxEntity.kt` | `EventEntity.kt` |
| DAO | `XxxDao.kt` | `EventDao.kt` |
| Component | 描述性名称 | `MonthView.kt`, `EventItem.kt` |

## 相关文档

- [RFC 5545 - iCalendar](https://datatracker.ietf.org/doc/html/rfc5545)
- [Android Architecture Guide](https://developer.android.com/topic/architecture)
- [Jetpack Compose](https://developer.android.com/jetpack/compose)
- [kizitonwose/Calendar](https://github.com/kizitonwose/Calendar)
